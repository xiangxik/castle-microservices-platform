server:
  port: 8001
management:
  security:
    enabled: false
spring:
  application:
    name: module-basic
  cloud:
    config:
      username: root
      password: qwe123
      discovery:
        enabled: true
        service-id: config-server
      profile: dev
      fail-fast: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/